## Agent skills

### Issue tracker

Issues and specifications are tracked in GitHub at `gweiermann/problem-tree-app`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five canonical triage labels plus a separate `defer` workflow label. See `docs/agents/triage-labels.md`.

### Domain docs

The repository uses a single-context domain-documentation layout. See `docs/agents/domain.md`.
